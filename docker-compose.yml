---
version: "3"

services:
  backend:
    image: tptacs.azurecr.io/backend
    build: ./backend
    ports:
      - "443"
    links:
      - mongodb
    depends_on:
      - mongodb
  frontend:
    image: tptacs.azurecr.io/frontend
    build: ./frontend
    ports:
      - "80"
  mongodb:
    build: .
    image: tptacs.azurecr.io/mongo
    container_name: mongo
    ports:
      - "443"
