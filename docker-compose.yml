---
version: "3"

services:
  backend:
    image: backend:${TAG:-entrega1}
    build: ./backend
    ports:
      - "8080:8080"
  frontend:
    image: frontend:${TAG:-entrega1}
    build: ./frontend
    ports:
      - "5173:5173"